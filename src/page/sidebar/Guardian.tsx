import React, { Component } from 'react'
import { observer } from 'mobx-react'
@observer
export default class Guardin extends Component {
    render() {
        return (
            <h1>Guardian</h1>
        )
    }
}